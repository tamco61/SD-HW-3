version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./configs:/app/configs
    environment:
      - CCACHE_DIR=/app/.ccache
